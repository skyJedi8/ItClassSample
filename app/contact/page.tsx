import Section from '@/components/Section';import QuoteForm from '@/components/QuoteForm';import { CallButton, TextButton } from '@/components/CTAButtons';import { getMetadata } from '@/lib/seo';import { localBusinessSchema } from '@/lib/schema';import { siteConfig } from '@/lib/site';
export const metadata=getMetadata('Contact Operation Clean Freedom | Get a Fast Quote','Call, text, or request a quote for exterior cleaning services in Houston and surrounding communities.','/contact');
export default function Page(){return <main><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(localBusinessSchema)}}/><Section><h1 className="text-4xl font-bold">Get a Fast Quote</h1><div className="mt-4 flex gap-3"><CallButton/><TextButton/></div><p className="mt-4">Hours: {siteConfig.hours}</p><p>Email: {siteConfig.email}</p><div className="mt-8"><QuoteForm/></div></Section></main>}
