import AreaPage from '@/components/AreaPage';
import { getMetadata } from '@/lib/seo';
export const metadata = getMetadata('Exterior Cleaning in Spring, TX | Operation Clean Freedom','Gutter, window, roof, pressure washing, and drainage cleaning in Spring, TX.','/service-areas/spring');
export default function Page(){ return <AreaPage city="Spring" />; }
